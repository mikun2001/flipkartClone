import { Link } from "react-router-dom";
import { Button, Divider, Box, Typography, styled } from "@mui/material";


const ContentLink = ({ products }) => {
	return (
		<>
			<h1>MOST SEARCHED FOR ON FLIPKART:</h1>
			
		</>
	);
};

export default ContentLink;
